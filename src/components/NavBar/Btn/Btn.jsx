import React from 'react'
import NavBar from '../NavBar'

const Btn = ({NavItem}) => {
    function selectOption(){
        
    }
  return (
    <button className='bars' onClick={()=> {selectOption()
        
    }}>click</button>
  )
}

export default Btn